{
  "name": "minutes-app",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -n worker,frontend -c blue,green \"npm run dev -w worker\" \"npm run dev -w frontend\"",
    "install:all": "npm install -w worker && npm install -w frontend"
  },
  "workspaces": [
    "worker",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
